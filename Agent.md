# Agent.md — 协作手册（AI 助手 / 新开发者 · 跨语言通用）

> **定位**：本仓库的**协作铁律 · 标准工作流 · 架构原则** —— 与语言、框架、项目无关，可整份复制到任意新项目作为起点。
> **不重复原则**：项目身份 / 骨架 / 技术栈 → `docs/013.project-info.md`；编码细则 → `docs/015.coding-standards.md`；报文形态 → `docs/016.api-contract.md`；文档体系与编号规则 → `docs/011.agreements.md`。冲突时一律以上述文档为准。

---

## 011.项目速览

- **一句话**：`mobile-admin` —— 企业级移动端管理系统；React + TypeScript + Vite，单文件构建交付。
- **对外契约冻结**：路由前缀 / 接口结构 / 响应信封 / 表命名，**禁止破坏性变更**。
- **权威源**：背景 / 目标 / 仓库骨架 / 技术栈 → `docs/013.project-info.md`；**模块与依赖链** → `docs/infrastructure011/011.topic - infrastructure.md` §013。

> 本手册只写**怎么做**（协作规则，语言无关）；**这是什么项目**归 `docs/` 四件套。

## 013.知识库地图（按顺序读）

1. `README.md`（仓库根）—— **仓库入口**（总览 / 能力清单 / 快速开始 / 文档导航）
2. `docs/README.md` —— **文档索引**（顶层四件套 + 常驻目录 + 流水线）
3. `docs/011.agreements.md` —— **文档体系与编号约定**（目录语义 / 编号规则；跨语言通用）
4. `docs/013.project-info.md` —— **项目信息**（背景 / 目标 / 骨架 / 模块 / 技术栈，**中文**）
5. `docs/015.coding-standards.md` —— **编码标准**（技术栈 / 组织方式 / 命名 / 风格 / 注释）
6. `docs/016.api-contract.md` —— **接口通讯规则**（统一信封 / 分页 / 查询 / 写操作 / 出入参形态；**只定义报文形态，不枚举端点**）
7. `docs/infrastructure011/` —— 整体底层架构设计（横切视角；入口为其 `README.md`）
8. `docs/sql/` —— **DDL 唯一真源**（`*.sql` 按文件名排序执行）

## 015.铁律

### 011.提交时机

- 只有用户明确发出「提交」「推送」「push」指令时，才能执行提交脚本
- 禁止主动提交代码

### 013.提交前置检查

提交前必须完成以下两项检查，缺一不可：

- **构建与静态检查通过**：`npm run build` 无报错 + TypeScript 类型检查通过
- **接口与逻辑自测验证通过**：核心功能及新增接口必须经过功能测试验证

### 015.Git 操作权限与分支管理

- **只读及本地状态操作**（`git pull` / `status` / `diff` / `log` / `branch` / `checkout` / `switch`）助手可自主执行
- **写操作**（`git add` / `commit` / `push`）必须且只能通过提交脚本统一执行，**禁止直接使用原生 `git` 命令进行提交**

### 016.助手权限边界

- **可自主执行**：编译、运行测试、启动服务、接口自测验证、只读及分支切换类 Git 操作
- **高危与提交操作**：执行提交脚本、删除文件 / 分支、数据库 destructive 变更必须经用户明确授权
- **轻量变更特例**：仅修补拼写、单行注释等微小改动，可合并至当前 Task 汇报，免除独立审批

### 017.HTTP 方法规范

- 业务接口原则上统一使用 **POST** 方法，入参采用 Request Body (JSON) 结构
- **GET** 仅用于无参或简单 URL 参数的纯查询场景
- 带条件的查询端点统一 **POST + JSON body**（条件放 body）；无参 / 简单 URL 参数的纯查询可用 GET
- 同步（sync）、更新（update）、删除（delete）等状态改变类接口一律使用 **POST**
- 禁止把参数塞进 URL 路径段（路径只承载资源定位，如 `/{module}/v1/{action}`）；条件与数据一律走 body
- 报文形态、统一信封与出入参样例见 `docs/016.api-contract.md`

### 018.版本号管理

- 工作日志与提交信息中的版本号必须与 `package.json` 中的版本号严格保持一致，禁止人工篡改

## 016.执行环境（Linux + Windows 双栖）

本仓库在 **Linux 与 Windows 两套环境并行开发**，约定必须两侧同时成立：

- 助手执行命令**必须直接在本机执行，禁止使用沙箱**
- 编译 / 测试等长命令一律**脱钩执行**：后台运行 + 输出重定向到日志文件，再从文件读取结果；**禁止让会话等待长输出**
  - Linux：`setsid nohup <cmd> > logs/build.log 2>&1 &`
  - Windows（Git Bash）：`<cmd> > logs/build.log 2>&1 &`
- 卡死的进程按需强制清理后再重试
  - Linux：`kill -9 <pid>` / `pkill -f <pattern>`
  - Windows：`taskkill //F //PID <pid>`（Git Bash）
- **换行符一律 LF**：脚本 / 配置 / 源码统一 LF，由 `.gitattributes` 强制，**禁止 CRLF 进仓**
- **编码一律 UTF-8（无 BOM）**
- **禁止硬编码绝对路径**：源码 / 配置 / 脚本内一律相对路径或环境变量占位

## 017.标准开发流程

收到需求后，必须严格遵循以下标准流水线执行：

```text
[步骤 1] 记录对话 -> docs/{YYYY-MM-DD}-chat.md
   ↓
[步骤 2] 分析需求 -> docs/infrastructure011/ (整体底层架构设计) + docs/requirement011/ (业务设计与业务要求) -> docs/requirement013/ (技术方案) -> docs/requirement015/ (技术实现)
   ↓
[步骤 3] 拆解任务 -> 写入 docs/{YYYY-MM-DD}.md (Todolist) 并提示用户确认
   ↓
[步骤 4] 用户批准 -> 按 Todo 逐项编码开发
   ↓
[步骤 5] 质量验证 -> 构建 + 测试 + 接口自测（命令见 docs/015.coding-standards.md）
   ↓
[步骤 6] 成果汇报 -> 向用户汇报自测结果与 Task 完成状态
   ↓
[步骤 7] 执行提交 -> 仅在收到用户「提交 / Push」指令时，调用脚本提交
```

**代码先行审查（强制）**：对于涉及底层公共库修改、多模块联动或破坏性重构的操作，在动手前必须先在对话中向用户提炼汇报**设计思路 / 关键变更路径 / 潜在风险点**，获得认可后再开始编码，避免方向性误偏。

## 018.架构与分层原则（语言无关）

分层与依赖约束是**跨语言通则**，与具体框架无关；本仓的落地实例见 `docs/013.project-info.md` §018 与 `docs/015.coding-standards.md` §013。

- **依赖单向（硬约束）**：模块 / 层之间只允许单向依赖，**禁止反向依赖与依赖回环**
- **分层职责**：接入层（HTTP / RPC 适配、入参校验、出参封装）→ 业务层（业务编排、事务边界、依赖装配）→ 数据层（持久化映射）；**上层可调下层，下层不得反向感知上层**
- **事务边界在业务层**：事务只允许出现在业务层方法上，一个业务操作一个事务
- **底层轻依赖**：公共基础模块（常量 / 工具 / 分页 / 异常 / HTTP）仅引入必要轻量依赖，不引入重量级框架
- **业务模块 = 包**（限界上下文 = 包）：按业务域分包，每包内自带完整的接入 / 业务 / 数据 / 模型分层，**禁止大杂烩包**
- **依赖注入**：构造器注入为主，**禁止字段注入**
- **SPI 抽象**：跨实现的扩展点（存储 / 认证 / 密码等）用接口定义在基础模块，实现注册在子包；业务依赖接口，**禁止依赖具体实现**
- 修改模块时必须严守既定的分层落位与编码风格，禁止混用风格

## 019.命名总原则

- **数据库与 SQL**：采用 snake_case
- **代码**：类 / 类型使用 PascalCase，方法 / 变量使用 camelCase（**方法名禁用下划线**），常量 UPPER_SNAKE_CASE
- **URL 动作段与 JSON 键**：一律 **camelCase**（铁律：`/logicDelete`；统一信封 `statusCode` / `errorMessage` / `traceId` —— 见 `docs/016.api-contract.md`）
- **配置文件 / 路径**：使用下划线或连接符（禁用空格），保持环境间配置命名统一
- **组件命名**：React 组件 PascalCase，文件名与组件名一致
- **路由路径**：kebab-case（`/user-management` `/role-permission`）

## 020.红线（速查）

- **禁止直接 `git add` / `commit` / `push`** —— 一律走提交脚本
- **对外契约禁止破坏性变更** —— 路由前缀 / 接口结构 / 响应信封 / 表命名
- **文档改号必须同步全仓引用** —— `docs/*.md`、`Agent.md`
- **堆栈 / SQL 不出站** —— 失败响应只回安全消息，细节只进日志
- **高危操作先授权** —— 提交、删除文件 / 分支、数据库 destructive 变更

## 021.语言栈约定

- 语言与框架细则（技术栈 / 分层组织 / 命名 / 风格 / 注释 / 测试）**一律下沉**至 `docs/015.coding-standards.md`，本手册不重复、不覆盖
- 本仓语言栈为 **TypeScript + React 18 + Vite 6**，其技术栈、模块与语言约束见 `docs/013.project-info.md`
- 任何语言栈都必须满足本手册 §015 铁律、§018 分层原则与 §019 命名总原则；语言专属内容不得写进本手册
